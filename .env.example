# Environment example for Budget Wise
# Copy this file to `.env` in the repo root and adjust values as needed.
# Docker Compose automatically reads variables from `.env` at the project root.

# ----------------------
# PostgreSQL (used by docker-compose services)
# ----------------------
# Database credentials
DB_NAME=budget_db
DB_USER=budget_user
DB_PASSWORD=budget_pass
DB_PORT=5432


# ----------------------
# Frontend (Next.js)
# ----------------------
# Server-side base URL for the backend; used by Next.js (e.g., in rewrites or server-only code).
# Local dev (backend on host):
API_BASE_URL=http://localhost:8000
# Docker Compose (backend service name is `backend`): uncomment to use inside Compose
# API_BASE_URL=http://backend:8000
